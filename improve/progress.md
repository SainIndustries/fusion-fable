# Fusion self-improvement loop — progress log

Append-only. One line per state transition, written by `improve/run_iteration.sh`. This survives context
resets, so the loop (and you) can reconstruct what happened by reading this plus `roadmap.json`/`state.json`.
